# smart-rate-limiter
Node.js + TypeScript backend exploring a hybrid rate limiter — fast heuristic abuse detection with AI fallback for ambiguous traffic, built against an Auth + OTP Notification service.
